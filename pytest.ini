[pytest]
testpaths = code/tests
python_files = test_*.py
addopts = -v --tb=short -s
# -s is required because pytest capture module has a Bad file descriptor
# bug on Python 3.14 + Windows. Tests themselves are fine.
