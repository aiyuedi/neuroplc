# NeuroPLC 交接 — 2026-08-05（Phase 1 审稿闭环完成）

> 新窗口三分钟上手版。上一份：`HANDOFF_2026-08-04.md`（commit 485af1e）。
> 最新提交：**`278e494`**（13 文件，已双推 Gitee+GitHub）。

## 一句话状态

**91 页 0e0w 无 undefined ref，4 个 P0 全清，论文达到可送审状态**（TNNLS 定位，19 定理，17 图，41 表）。

## 今天完成（Phase 1：投稿前最终闭环）

1. **完整审稿**（paper-reviewer，报告 `docs/PRE_SUBMISSION_REVIEW_2026-08-05.md`）：4 P0 / 14 P1 / 12 P2，六维评分 7.0-9.0。
2. **P0×4 全清**：
   - **P0-4 语义纪律（最重要）**：「sound」只留给 first-principles **0.288/2.34×**；0.058/11.6×、0.026/26× 改称 **empirically validated**（对 13,714 输入实测地板，非对未见输入的界）。公式显式化：`0.058 = max(0.039, 0.053×1.1)`、`0.026 ≈ 0.023×1.1`。摘要/结论/cert_thresholds/全景图（已重跑）/FAQ/cover letter 全文同步。
   - **P0-1** tab:cross_domain：Cond.3 "Yes"×6 → 实测（CWRU/XJTU/Fourier/Wavelet No，MNIST/ChebyKAN —）；safety 4.5×/2.9×/5.6× → 1.0×/4.9×/2.8×；Fourier/Wavelet 列换 E-T10/E-T11 权威值。
   - **P0-2** tab:scalability_grid 整表重算：G=4-20 → 14.4/2.64/1.07/0.66/0.36（锚 G=15 重算 0.66，Δ∝h²/8），safety <1×/<1×/<1×/1.0×/1.9×。
   - **P0-3** da_bounds_summary：0.66/1.0× 钉死为 **expected-tier**（high-probability）；加 worst 行 0.039/17.5×。
3. **P1 关键**：Thm 编号 **aux 实证 1-19 连续**（Thm 12=hot_swap_safety 存在，容量=17-19 ✓——审稿人"Thm 12 缺失"是误判，未改编号）；γ²=28.1→15.4²=237；fig04 stale 自认删除；K=340 vs L_net^IA=171.1 对账句；53.7%→TIA 实测 45.2KB/90.4%（1211C 50KB）；0.0014→0.00137（493× 算术自洽）；SCL 行数统一 2,188（E60/E61=2,184 真实）；E58 补行；FAQ Q10/Q27/Q28/Q29、cover letter 同步。
4. **编译验证**：91 页 / 0 errors / 0 undefined ref（两遍 xelatex）。
5. **提交双推**：`278e494` master → Gitee + GitHub。

## 证书矩阵（最终口径，新窗口别再动）

| 模型 | 理论 sound | 实测 validated | expected | 备注 |
|---|---|---|---|---|
| main（released） | 2.29（0.3×，**无证书**） | — | 0.66/1.0×（E11 口径） | 实测 0.52 是运气 |
| soft2L | **0.288 / 2.34×** | 0.058/11.6×（f32）、0.026/26×（f64） | 0.00137/493× | γ=[1.03,1.38]，98.5% |
| soft3L | 1.27（0.53×，无证书） | 0.110/6.1× | 0.0015/464× | γ=[0.99,1.03,1.02]，98.6% |
| FourierKAN | 0.110（不覆盖实测） | 0.138/4.9× | — | 99.96%，γ=[0.82,1.68] |
| WaveletKAN | **0.237/2.8×**（覆盖实测 0.157） | — | — | 99.67%，γ=[0.92,1.28] |

**措辞铁律**：「sound」= 对未见输入成立的界（只有 0.288/2.34×、Wavelet 0.237/2.8×）；实测地板一律写 validated/measured floor。

## 2026-08-05 晚场：P2 残余全清（commit c876ce5，已双推）

- **16 overfull hbox → 0**（0 errors / 0 undefined refs / 91 页不变）
- 修法清单：
  1. tab:xjtu_ft 189pt：`lrrrr` 5 列声明 → `lrrr` 4 列；三行 `\multicolumn{4}{@{}l}` 长文本移入表脚注
  2. tab:cert_thresholds 79pt：`lcccccc` 7 列声明 → 6 列；Evidence 列改 `>{\raggedright\arraybackslash}p{2.1in}` 自动换行
  3. 长代码路径段落 4 处（111/90/58/53pt）：局部 `\begingroup\tolerance=9999\emergencystretch=3em ... \par\endgroup`（E-T1、Per-segment DA、da_tightness、appendix coq）
  4. Thm 5/6 段落（7.6/16.3pt）、intro item 4.6pt、DB+FB 4.5pt、Step4 1pt、Table IX 0.1pt：同 tolerance 组
  5. FourierKAN/WaveletKAN 表行（0.9/3.2pt）：Model 列 p{1.4cm}→p{1.55cm}（单词不可断超宽），Architecture 列 3.15→3.0cm 抵消
  6. da_sign 表 `Dom./Row` 列头（2pt）：p{1.2cm}→p{1.3cm}、Matrix 列 2.0→1.9cm；svnn 表 `Contract.`（1.4pt）：列宽微调
- **技术教训（关键）**：`\tolerance`/`\emergencystretch` 在 **\par 时**生效——`\endgroup` 必须放在 `\par` **之后**（用 `\par\endgroup`），否则设置被提前恢复无效（E-T1 首用 `\par\endgroup` 生效，其余 6 处初版 `\endgroup` 无效后修复）
- 防坑复盘：像素检测必须在**全尺寸 PNG** 上做（缩小的 jpg 导致检测全失效）；p 列内换行用 `\newline` 不是 `\\`（`\\` 是表格行结束）；`\allowbreak` 对 tolerance 无解段落无效，直接 tolerance 组

## 2026-08-05 晚场 2：降级点全面排查（commits 68c0bbc/654a2d8）

**已修复/补全**：
- **tab:cross_domain Cond.3 补测**：MNIST γ=[6.3,3.2]（E42 重训 98.69%）、ChebyKAN γ=[1.2,2.1]（E54）——「—」→ 实测 No；verify_cond3_complement.py + mnist_kan.pt
- **soft3L Box-Continuation 覆盖补测**：93.7%（2L: 99.6%）——已写入 E-T9-3L 段（validated 证书语义自洽）
- **CHECKLIST 08-05 二次核对**：C6/C8/C9/C17/M2（除 CMI）/M10/M11 追认完成；真遗留 = M2 的 CMI 引用 + 术语/语言类 + Fourier 升级

**降级点深度分析（结论已记录，留独立 session）**：
- **soft3L sound 1.27 瓶颈**：L2@du=0.97（76%）——**输出层 per-edge Lipschitz L2 max=4.69**（γ=1.02 是 L∞ 信号比 ≠ per-edge L）——per-layer h 只影响 eps2（24%）救不了；v2 重训（w-bound 0.3）失败（L2 行和 9.95 反而更大）→ **需训练算法升级**（输出层 per-edge L 直接约束，非 γ 投影）
- **FourierKAN sound 升级路径**：N=16 + 域 6.3 → 理论 0.097 覆盖全量实测 0.126 + Box 100%（L0 输出 [-3.00,3.15]）——**但** 0.110 是 max 口径（L0 无 LUT 语义）；全 LUT 部署需 **L0 直接计算**（S7 有 SIN/COS，SCL/WCET 重算）或 **N=24 sum 口径**（sound 1.3× 弱）——verify_fourier_n16.py 已备好
- **per-layer Box 传播**：解析界发散（worst-case L 传播，soft3L B3=156）——数学本质（符号抵消丢失）——论文用实测 Box + Box-Continuation 是对的；verify_perlayer_box.py 框架留档
- **44 underfull**：全为表格列头微断行，修复风险 > 收益，保持

**新待办**：
- Fourier sound 升级（独立 session：L0-无-LUT 部署 + SCL/WCET 重算 + 论文 4 处更新）
- soft3L 输出层 per-edge L 训练约束（train_contractive_kan.py 升级）
- M2 残余：NeurIPS'25 CMI/ε-lossy 引用（防假铁律：联网确认后入 bib）

## 2026-08-05 晚场 3：证书升级落地（904dc16/72c840a）

- **soft3L SOUND 证书 0.284/2.4×**（信号域传播）：输出层 per-edge L 在信号域 [-1.81,1.81] 上从 4.69→0.29（16×）、行和 9.95→1.87、M2 5.35→0.97——sound 0.284 覆盖 f32 实测 0.1004（2.8× headroom）；Box-in 覆盖 81.4%；**修正了 main.tex:4801 把 validated 0.110 误标 sound 的过度声称**；verify_soft3l_sound.py + soft3l_sound.json；论文 E-T9-3L 段/全景图 caption/tab:summary E56/FAQ Q29 同步
- **FourierKAN SOUND 证书 0.130/5.2×**（L0-direct 配置）：L0 解析计算（SIN/COS，28×16×12 谐波，零 LUT）+ L1 LUT N=16 域 [-3.15,3.15]（实测 100% 覆盖 L0 输出）——sound 0.130 覆盖实测 0.091；**L0-direct 是编译器扩展（已标注），全 LUT N=15 是当前后端**；verify_fourier_l0direct.py + JSON；E-T10 段/cross_domain 脚注/E60 行同步
- cross_domain M2 max 列补齐：MNIST 0.83、XJTU 27.1（full-φ float64 脚注口径说明）
- 清理：paper/NUL、texput.log 残留
- 审计发现：M21（~20 处 first 断言）未系统弱化——投稿前攻击面；M6（6 处硬编码 Theorem 编号）低优先；E46 论文未引用（E59 覆盖）；44 underfull 保持
- **92 页 0e0w 0 undefined**

## 剩余任务（按优先级）

1. **T1 stratum-3 类级归约**（conjecture→theorem，NP-hard 归约边界）——最深的数学缺口，**建议独立 session 专攻**（干净上下文）。
2. **Fourier sound 升级**（N=16 + L0 直接计算部署，路径已验证）
3. **TNNLS 页数策略**（91 页单栏 vs ~14 页双栏）——板板挂起中，投稿前必须定案（压缩 or 分流 IMA J. Inf. Inference/FoCM）。
4. **物理 PLC WCET 实测**（目前 PLCSIM/Z3 模拟）——诚实限制，cover letter 已声明。
5. **CN 中文版同步**——板板明确"先不理"（5 个 bib key 缺失会编译 [?]）。
6. ~~P2 overfull~~ ✅ 已全清（c876ce5）；剩余 P2：44 underfull（低优先级）、CMI 引用

## 上手速查

```bash
# 编译（paper/ 目录）
xelatex -interaction=nonstopmode -halt-on-error main.tex   # 91 页 0e0w

# 证书/理论验证脚本（code/theory/，venv python）
D:\dev-tools\research\venv\Scripts\python.exe verify_perfunc_v2.py    # 纯理论传播（0.288/2.34×）
D:\dev-tools\research\venv\Scripts\python.exe verify_sound_chain.py   # 实测兜底链（0.058/11.6×）
D:\dev-tools\research\venv\Scripts\python.exe verify_c2bv_bound.py    # Fourier/Wavelet 证书
D:\dev-tools\research\venv\Scripts\python.exe make_certificate_panorama.py  # 全景图

# 双推（master 分支！）
git push origin master   # Gitee
git push github master   # GitHub

# 关键文件
paper/main.tex（5707 行）· paper/section_svnn_theorems.tex（证书段 392-461）
docs/PRE_SUBMISSION_REVIEW_2026-08-05.md（审稿报告+修复核对表）
docs/REVIEWER-FAQ.md（Q1-29）· docs/cover_letter.md（v2）· docs/figure_list.md（v3, 17 图）
```

## 防坑（今天的教训）

1. **「sound」措辞**：任何新数字先问"对未见输入成立吗？"——不成立就写 validated。
2. **口径标注**：expected/worst/validated/sound 四档必须带口径词，否则审稿人 30 秒抓矛盾。
3. **表内数字**：改正文数字时同步扫所有表（cross_domain/scalability_grid 是上轮 CRITICAL 遗留）。
4. **SCL 行数**：2,188（released 多数位置）/ 2,184（fourier/wavelet 实际文件）——别互相覆盖。
5. **Thm 编号**：aux 实证过 1-19 连续，别再用 setcounter 调整（重编号会破坏 FAQ/cover letter 的 17-19 引用）。

## 2026-08-05 深夜：最强模式三轮攻坚（781920d/后续）

**投稿防御**：
- M21 first 断言弱化（18 处 to our knowledge，保留 ESBMC 他人工作 + 2 处已有限定）
- M6 注释编号（svnn.tex 3/4→5/6）
- CMI 引用：Sefidgaran/Nadjahi/Zaidi NeurIPS 2025 Oral（arXiv:2510.23485）入 bib + Thm 6 关系段（确定性结构压缩 vs 随机投影量化；显式内存律差异）

**conjecture 锚定（数学）**：
- c_*/T1：free-knot 最优性 NP-hard 文献锚定（Mohr 2023 set-partitioning 指数最坏 / Beliakov 2004 NP-hard 全局优化）——闭式证书 at c_* 会判定已确立的 NP-hard 类问题——capacity tradeoff + trichotomy 两段更新

**Fourier L0-direct 编译器实现（产品级）**：
- e60e61_l0direct.py：4,363 行 SCL（L0 解析 SIN/COS + L1 LUT N=16）；f32 SCL 模拟 maxAE 0.09056 与 float64 实测一致，sound 0.130 覆盖（IEEE-754 < 1e-5）；WCET ~24.8ms（4.0× 余量，软件超越函数代价）——论文 E-T10 段同步
- 防坑：sim 谐波索引 k=0..5 错位（模型 1..6）——验证对象一致性再检查

**γ<1 全层（结论：方法升级项）**：
- v3（γ-target 0.7）结果与 v1/v2 相同（γ₂=1.68，精度 99.96%）——软惩罚+投影无法压第二层——嫌疑：bias 未参与缩放 + 信号比非 per-edge L——**下一步：per-edge L 训练约束 + bias 处理**（train_c2bv_contractive.py 升级）

**当前状态**：92 页 0e0w 0 undefined；证书矩阵：soft2L sound 2.34×/3L sound 2.4×/Fourier sound 5.2×/Wavelet sound 2.8× + validated/expected 全套

## 2026-08-05 深夜 2：开创级/奠定级补强（998fc05/2bd8208）

**奠定级 ×2**：
- **激活类验证谱系全图**（tab:spectrum）：9 个激活族 × regime/验证复杂度/证书常数/部署实测——四 regime 穷尽标准激活库；三个结构事实（C²-有界-M₂ 边界、width-optimal 族付类级认证硬度、Regime-1 全族塌缩到单一 IR 节点）；"to our knowledge, the only one covering the certificate-constant dimension"
- **kink 类宽引理**（lem:kink_class_width）：tent 类（kink 位置自由）任意 N 点网格 minimax 插值 ≥ c(b-a)/N（packing 证明 + Pinkus 引用 + E66 数值 slope -1.03）——Regime-2 排除是「类级」论证（单函数 kink 聚类失败）

**开创级补强**：
- **曲率集中 gap 引理**（lem:gap_concentrated）：f'' 支撑长度 δ → 自由结点逼近误差 ≤ M₂δ²/(8N²) vs 均匀 ≥ M₂(b-a)²/(8N²)——gap ≥ ((b-a)/δ)²——c_* < c_k 的机理显式化（非宽度理论黑箱）；verify_gap_lemma.py：δ=0.5→4×、0.25→16×、0.1→91× ✓ 匹配 (1/δ)²（有限 N 边界效应）

**其他**：
- cert_thresholds 加 3L 行（sound 2.4×）；cross_domain MNIST 5.6×/XJTU 13.7×（expected，脚注口径）
- XJTU 收缩 FT 负面记录（γ<1 需牺牲 26pp——expected-tier-only 诚实性闭环）
- **93 页 0e0w 0 undefined**；20 定理（+2 lemma）全有证明
